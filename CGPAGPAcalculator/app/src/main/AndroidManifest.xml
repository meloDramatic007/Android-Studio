<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.saddan.cgpa_gpacalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GPA_Activity" />
        <activity android:name=".GPA_ListView_activity" />
        <activity android:name=".Subject_Grade_GP_adapter" />
        <activity android:name=".CGPA_Activity" />
        <activity android:name=".Subject_Credit_GPA_Total_adapter"></activity>
    </application>

</manifest>